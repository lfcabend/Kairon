{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kairon.serviceAccountName" . }}
  labels:
    {{- include "kairon.labels" . | nindent 4 }}
{{- end }}
